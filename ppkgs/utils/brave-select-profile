choice=$(cat "${HOME}/.config/BraveSoftware/Brave-Browser/Local State" | \
  jq -r '.profile.info_cache | to_entries[] | "\(.value.name) (\(.key))"' | \
  dmenu | \
  sed -n 's/.*(\([^)]*\)).*/\1/p')

brave --profile-directory="${choice}"
